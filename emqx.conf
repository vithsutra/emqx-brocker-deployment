## Disable anonymous authentication
allow_anonymous = false

## Enable the built-in authentication database
auth.mnesia.enable = true

## Define user credentials
auth.user.1.username = mqttuser
auth.user.1.password = mqttpassword

## Hash password (optional but recommended)
auth.user.1.password_hash = plain  # Use `sha256` or `bcrypt` for security
